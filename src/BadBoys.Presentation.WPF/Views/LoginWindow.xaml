<Window x:Class="BadBoys.Presentation.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" 
        Height="300" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Personal Media Collection Manager" 
                   FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Username:"/>
            <TextBox x:Name="UsernameTextBox" Height="25"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <Label Content="Password:"/>
            <PasswordBox x:Name="PasswordBox" Height="25"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Login" Width="80" Margin="5" Click="LoginButton_Click"/>
            <Button Content="Register" Width="80" Margin="5" Click="RegisterButton_Click"/>
            <Button Content="Exit" Width="80" Margin="5" Click="ExitButton_Click"/>
        </StackPanel>
        
        <TextBlock Grid.Row="4" x:Name="MessageText" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>

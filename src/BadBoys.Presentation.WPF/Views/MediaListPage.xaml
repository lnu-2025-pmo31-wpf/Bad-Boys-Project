<Page x:Class="BadBoys.Presentation.WPF.Views.MediaListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:BadBoys.Presentation.WPF.ViewModels"
      Title="MediaList">

    <Grid Margin="10">
        <DockPanel>

            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 10">
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Width="90" Margin="0 0 10 0"/>
                <Button Content="Add" Command="{Binding AddCommand}" Width="90"/>
            </StackPanel>

            <ListView ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Id" Width="50" DisplayMemberBinding="{Binding Id}" />
                        <GridViewColumn Header="Title" Width="200" DisplayMemberBinding="{Binding Title}" />
                        <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding Type}" />
                        <GridViewColumn Header="Rating" Width="80" DisplayMemberBinding="{Binding Rating}" />
                    </GridView>
                </ListView.View>

                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Edit" Command="{Binding EditCommand}"/>
                        <MenuItem Header="Delete" Command="{Binding DeleteCommand}"/>
                    </ContextMenu>
                </ListView.ContextMenu>

            </ListView>

        </DockPanel>
    </Grid>
</Page>

<Window x:Class="BadBoys.Presentation.WPF.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register New Account" 
        Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Create New Account" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0 0 0 20"/>
        
        <TextBlock Grid.Row="1" Text="Username:" Margin="0 0 0 5"/>
        <TextBox x:Name="txtUsername" Grid.Row="2" Height="30" 
                 Padding="5" Margin="0 0 0 15"/>
        
        <TextBlock Grid.Row="3" Text="Password:" Margin="0 0 0 5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="4" Height="30" 
                     Padding="5" Margin="0 0 0 15"/>
        
        <TextBlock Grid.Row="5" Text="Confirm Password:" Margin="0 0 0 5"/>
        <PasswordBox x:Name="txtConfirmPassword" Grid.Row="6" Height="30" 
                     Padding="5" Margin="0 0 0 20"/>
        
        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Cancel" 
                    Width="80" Height="30" Margin="0 0 10 0"
                    Click="CancelButton_Click"/>
            <Button x:Name="btnRegister" Content="Register" 
                    Width="80" Height="30"
                    Click="RegisterButton_Click"/>
        </StackPanel>
        
    </Grid>
</Window>
